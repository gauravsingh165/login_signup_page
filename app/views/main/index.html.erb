<div class="d-flex align-items-center justify-content-center">
  <h1> This is main Page</h1>  
</div>
<div class="d-flex align-items-center justify-content-center">
  <% if @user %>
    <h5 style="color: green;">Logged in as <%= @user.email %></h5>
    <%# <%= button_to "Logout", logout_path, method: :delete%> 
  <% end %>
</div>
